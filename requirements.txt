# IMPORTANT: Install PyTorch FIRST with CUDA support before other dependencies!
#
# For NVIDIA GPUs (RTX 3090, RTX 4090, etc.):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# For AMD GPUs (MI250X on HPC clusters):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm6.0
#
# Then install the rest of the dependencies below.

# Core numerical and scientific computing
numpy>=1.21.0
scipy>=1.7.0
scikit-learn>=1.0.0

# Image processing
opencv-python>=4.5.0
Pillow>=8.3.0
scikit-image>=0.18.0
h5py>=3.0.0

# Deep learning (install torch/torchvision separately with appropriate CUDA version)
# torch>=2.0.0 (install separately)
# torchvision>=0.15.0 (install separately)

# Segmentation models
cellpose>=3.0
git+https://github.com/facebookresearch/segment-anything-2.git

# CZI file reading
aicspylibczi>=3.0.0

# Progress bars and utilities
tqdm>=4.60.0

# Data visualization
matplotlib>=3.3.0

# Optional but recommended for monitoring
psutil>=5.8.0
